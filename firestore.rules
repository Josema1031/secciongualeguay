rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Solo pueden leer/escribir usuarios que hayan iniciado sesión
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
